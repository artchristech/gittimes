**Problem** — The AI Markets page at gittimes.com/markets (rendered by `src/markets.js` into `templates/markets.html`) is rated 1/10: its banner is a stale-looking timestamp stamp ("AI Markets / LIVE PRICING SYNCED FROM OPENROUTER · JUN 23, 11:02 AM MDT"), every section is an undifferentiated plain HTML table, there is zero evaluation/benchmark data, and the whole page is dead static — it reads like a spreadsheet dump, not a living newspaper. The goal is to overhaul this page into something professional, visually compelling, honest about data freshness, enriched with curated+cited model evals, and alive with HTML5 motion (a "Harry Potter newspaper" feel) — and to do it as a rigorous, independently-shippable phased delivery plan rather than one big risky rewrite.

**Context** — The Git Times is a zero-framework Node.js "AI newspaper for builders" that ships gittimes.com. There is NO build step for the site: the server string-templates HTML/CSS into static files via `applyTemplate` (`src/template-utils.js`); pages are plain CSS + vanilla JS only. A strict Content-Security-Policy lives in the page head — `script-src 'self' 'unsafe-inline'{{CSP_SCRIPT_SRC}}; connect-src 'self'{{CSP_CONNECT_SRC}}` — so external CDN scripts/libraries are FORBIDDEN unless you amend the CSP and self-host; inline `<script>` is allowed. The AI Markets page comes from two files: the shell `templates/markets.html` (347 lines; page-specific CSS is inline after `{{STYLES}}`; `.markets-header` holds the h1 + `.markets-date` freshness stamp; body is `{{MARKETS_CONTENT}}`; footer credits openrouter.ai) and the renderer `src/markets.js` (430 lines; `renderMarketsPage(tickerData, fullMarket, options)` emits all sections as plain tables: Cost of Intelligence Index with an SVG sparkline, Sunset Watch, Frontier Model Pricing, New on the Market, On Our Radar, Inference Speed Leaderboard, Image Generation, All Models by Provider; the freshness stamp is built from `tickerData.syncedAt`; helpers: `escapeHtml`, `renderSparkline`, `timeAgo`). These ~8 sections currently render as a wall of near-identical full-width tables with no hierarchy and lots of repetition — that redundancy is a core reason the "display of information is terrible"; consolidating and re-ranking them (so a builder can answer price-vs-quality-vs-speed at a glance, with secondary/long data behind progressive disclosure) is in scope, not just re-skinning. Pricing data is synced into `data/ai-models.json` (~10k lines). Editorial curation lives in `data/ai-models-curated.json` (`bannerKeys`, `trackedModels[]`, `speed[]`, `images[]`). There is NO eval/benchmark data anywhere yet — a phase must ADD a curated, cited evals dataset (extend the curated JSON, e.g. an `evals` array per model with MMLU-Pro / GPQA / SWE-bench / Arena-Elo + a `source` URL + an `asOf` date) and render it; like `speed`/`images`, evals must be honestly labeled as curated + dated and MUST NEVER be presented as live or fabricated. Global styles + theme tokens are in `styles/newspaper.css` (CSS vars `--paper`/`--ink`/`--rule`/`--accent`/`--ink-faint`/`--font-headline`/`--font-meta`; `[data-theme]` light/dark/midnight). Local generate: `node generate.js`. Full publish: `npm run publish` (`src/publish.js` writes `outDir/markets/index.html`). Tests: `npm test` (existing tests under `test/`, e.g. `test/worker.test.js` — do not break them). Lint: `npm run lint`. Respect `prefers-reduced-motion`. Known dead ends: none yet — this is the first overhaul attempt.

**Inputs**
- Repo root: `/Users/christopherharris/projects/gittimes`
- Page shell template: `/Users/christopherharris/projects/gittimes/templates/markets.html`
- Page renderer: `/Users/christopherharris/projects/gittimes/src/markets.js`
- Data source (live pricing): `/Users/christopherharris/projects/gittimes/data/ai-models.json`
- Editorial curation (extend with `evals`): `/Users/christopherharris/projects/gittimes/data/ai-models-curated.json`
- Ticker/data layer: `/Users/christopherharris/projects/gittimes/src/ai-ticker.js`
- Global styles + theme tokens: `/Users/christopherharris/projects/gittimes/styles/newspaper.css`
- Templating util: `/Users/christopherharris/projects/gittimes/src/template-utils.js`
- Publisher: `/Users/christopherharris/projects/gittimes/src/publish.js`
- Existing tests: `/Users/christopherharris/projects/gittimes/test/` (e.g. `/Users/christopherharris/projects/gittimes/test/worker.test.js`)
- Local generate: `node generate.js` (run from repo root)
- Full publish: `npm run publish`
- Tests: `npm test` — Lint: `npm run lint`
- Output path written by publisher: `outDir/markets/index.html` (confirm exact `outDir` in `src/publish.js`) (unverified)
- Live page for reference: `https://gittimes.com/markets`
- The CSP placeholders `{{CSP_SCRIPT_SRC}}` / `{{CSP_CONNECT_SRC}}` are injected in the head of `/Users/christopherharris/projects/gittimes/templates/markets.html` (confirm exact location before editing).

**Constraints**
- Zero-framework, NO build step for the site: server-side string-templated HTML/CSS → static files. Plain CSS + vanilla JS only.
- CSP is hard: no external CDN scripts or libraries. Inline `<script>` and inline `<style>` are allowed. If you must add a script/connect source, you must amend the CSP placeholders AND self-host the asset within the repo — prefer pure inline solutions (hand-rolled inline SVG/CSS, extending the existing `renderSparkline` pattern) to avoid this entirely.
- Honesty is non-negotiable: live pricing stays live; curated data (speed, images, AND the new evals) MUST be visibly labeled as curated with a source and an `asOf` date. Never fabricate eval numbers or present curated data as live. If a model lacks a metric, render a clear "—"/"not benchmarked" rather than guessing.
- All motion must honor `prefers-reduced-motion: reduce` (animations disabled / reduced to static), and the page must be fully readable and information-complete with JavaScript disabled (animation is decoration, not the data path).
- Reuse existing theme tokens from `styles/newspaper.css` (`--paper`/`--ink`/`--rule`/`--accent`/`--ink-faint`/fonts); do not hardcode a parallel palette. Work across light/dark/midnight themes.
- Preserve all existing sections' information (don't drop data builders rely on); you may restructure, re-rank, consolidate, and re-skin. The masthead "The Git Times" and the mission footer line must remain.
- Do not break existing tests; `npm test` and `npm run lint` must pass after every phase. No new runtime dependencies.
- Each phase must be independently shippable AND reversible (a clean, self-contained diff that leaves the page in a working, deployable state on its own).
- BEFORE writing any code, you MUST write the comprehensive phased gameplan (see Definition of done item 1) to a file in the repo and have it cover every phase below.
- Each phase ends with its own acceptance check run and recorded before the next phase begins.

**Definition of done** — You must FIRST write a comprehensive gameplan file, THEN execute it phase by phase. Every item below is observable/checkable:

1. GAMEPLAN written first — A file `/Users/christopherharris/projects/gittimes/MARKETS_OVERHAUL_PLAN.md` exists and contains, for EACH phase below: a name, an ordered list of deliverables, the exact files touched, and a per-phase acceptance check command/observation. The plan is written and saved before any source file is edited.

2. Phase 0 — Audit & baseline. Deliverables: a "Current state" section appended to the plan enumerating each existing section in `src/markets.js`, the exact current banner markup in `templates/markets.html`, the current theme tokens used, and a captured baseline (run `node generate.js`, record output path + a screenshot or HTML snapshot of the current `/markets`). Acceptance check: `node generate.js` succeeds; `npm test` and `npm run lint` pass on the untouched tree; baseline artifact saved under the repo (e.g. referenced from `MARKETS_OVERHAUL_PLAN.md`). Independently shippable (no source change).

3. Phase 1 — Banner + freshness honesty. Deliverables: replace the outdated `.markets-date` stamp with a professional masthead/banner that shows relative freshness ("synced 3 min ago" via existing `timeAgo`) plus an absolute timestamp, a clear LIVE-vs-CURATED legend, and degrades gracefully when `syncedAt` is missing/stale (stale state visibly flagged). Files touched: `src/markets.js` (banner build), `templates/markets.html` (`.markets-header` markup/CSS). Acceptance check: regenerate; banner shows relative + absolute time and a live/curated legend; forcing a stale/missing `syncedAt` yields a visible "stale" indication (document the toggle used).

4. Phase 2 — Information architecture + visual redesign. Deliverables: re-architect the page around builder decisions (price vs quality vs speed / value) — collapse the prior wall of ~8 near-identical full-width tables into a coherent hierarchy with progressive disclosure for secondary/long data (no repeated full catalogs stacked on each other). Restyle each retained section from raw tables into professional newspaper-grade components (aligned tabular-nums numeric columns, rank/delta affordances, sparkline integration, responsive at mobile width) using only `newspaper.css` tokens; no parallel palette. Files touched: `src/markets.js` (section renderers + ordering), `templates/markets.html` (inline CSS), optionally shared tokens in `styles/newspaper.css`. Acceptance check: regenerate; the page no longer renders the prior set of redundant full tables; each retained section is visually distinct and aligned in light/dark/midnight; renders without horizontal overflow at ~375px width (verify in browser).

5. Phase 3 — Evals data + section. Deliverables: extend `data/ai-models-curated.json` with a curated, cited `evals` array per tracked model (e.g. MMLU-Pro / GPQA / SWE-bench / Arena-Elo, each with `source` URL + `asOf` date); add an "Evals / Benchmarks" section renderer in `src/markets.js` that displays them clearly labeled CURATED with sources and as-of dates, keyed to existing model keys/`openrouterId`s (do not fork a parallel model list). Files touched: `data/ai-models-curated.json`, `src/markets.js`. Acceptance check: regenerate; evals section renders with at least 3 models and ≥2 benchmarks each, every number carrying a visible source + as-of date; models without data show explicit "—"; no eval number presented as live.

6. Phase 4 — Living-newspaper motion. Deliverables: HTML5/CSS/inline-JS motion that makes the page feel alive within CSP (e.g. ticker/number count-up on load terminating on true values, sparkline draw-in, subtle row reveal, a "just updated" pulse on the freshness chip) — all pure inline, all gated behind `prefers-reduced-motion` and layered as progressive enhancement over server-rendered final values. Files touched: `templates/markets.html` (inline `<script>`/`<style>`), possibly `src/markets.js` for hooks/markup. Acceptance check: regenerate; motion is visible on load in a normal browser; with `prefers-reduced-motion: reduce` the page is static and fully readable; with JS disabled all final numbers/sections are intact; no CSP violations in the browser console.

7. Phase 5 — Verify & ship. Deliverables: full regeneration, all phases integrated, plan file updated with a final "Shipped" checklist marking each phase's acceptance check as passed. Files touched: none new (integration + plan update). Acceptance check: `npm test` passes, `npm run lint` passes, `node generate.js` succeeds, and a browser load of the generated `/markets` shows the new banner, redesigned/consolidated sections, evals section, and motion with zero CSP console errors.

8. Overall — No CSP violation appears in the browser console on the generated page. No external CDN script/lib was added (or, if a CSP source was amended, the asset is self-hosted in-repo and the amendment is documented in the plan). Live pricing remains live; all curated data (speed, images, evals) is visibly labeled curated + dated. The masthead "The Git Times" and the mission footer line are preserved. Existing tests still pass.

**Verification** — Run from `/Users/christopherharris/projects/gittimes`:
1. Confirm the plan exists and covers all phases: open `/Users/christopherharris/projects/gittimes/MARKETS_OVERHAUL_PLAN.md` and check each of Phase 0–5 has name + deliverables + files touched + acceptance check, and that it was saved before source edits.
2. `npm run lint` — must exit 0.
3. `npm test` — must pass, including pre-existing `test/worker.test.js`.
4. `node generate.js` — must succeed and write the markets page (note the output path it prints).
5. `node -e "const j=require('./data/ai-models-curated.json'); const e=j.evals||[]; if(!e.length) throw 'no evals'; for(const x of e){ if(!x.source||!x.asOf) throw 'eval missing source/asOf: '+JSON.stringify(x); }"` exits 0 (every eval entry cited + dated).
6. Open the generated `markets/index.html` in a browser (and `https://gittimes.com/markets` after publish if shipping): verify (a) banner shows relative + absolute freshness + live/curated legend; (b) the prior wall of redundant full tables is gone — sections are consolidated, redesigned, aligned, no horizontal overflow at 375px; (c) an Evals/Benchmarks section shows ≥3 models with ≥2 cited+dated benchmarks each, labeled CURATED, with "—" for missing values; (d) motion plays on load and fully stops under `prefers-reduced-motion: reduce`; (e) with JS disabled all sections + final numbers render; (f) the browser console shows zero CSP violations.
7. Toggle the theme through light/dark/midnight and confirm all sections use only `newspaper.css` tokens (no off-palette colors).
8. Force a missing/stale `syncedAt` (document the method) and confirm the banner shows a visible stale/missing indication rather than a misleading fresh stamp.
9. Confirm via `git diff` that the masthead "The Git Times" and the mission footer line ("…aims to provide information that inspires people to build.") are unchanged, and that each phase's diff is self-contained and left the tree in a passing, deployable state.

<!-- prompt-out v1 -->
